# Copy to .env.local for local dev, and add the same two as GitHub repo
# secrets named VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY for deploys.
# Leave both blank to run the app in DEMO MODE (all data stays on the device).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
